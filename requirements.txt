# ClawBot AI — Dependencies (minimal, trusted only)
# No ccxt, python-binance, ta, playwright, beautifulsoup4

# Core
pandas>=2.0.0
numpy>=1.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP
aiohttp>=3.9.0
httpx>=0.25.0

# AI (no SDK needed — using raw HTTP)
# groq, deepseek, gemini, claude, kimi all use httpx

# Database
supabase>=2.0.0

# Logging
loguru>=0.7.0

# Environment
python-dotenv>=1.0.0

# News Parsing
feedparser>=6.0.0
